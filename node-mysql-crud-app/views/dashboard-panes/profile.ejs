<div class="tab-pane active" id="profile" role="tabpanel" aria-labelledby="profile-tab">

    <h1 class="mt-2">Profile</h1>

    <!-- User profile -->
<!--
    <div class="card mb-3">
        <div class="card-body">
            <% if (users.fname && users.lname) { %>
              <h4 class="card-title"><%= users.fname %> <%= users.lname %></h4>
            <% } else { %>
              <h4>User's name not found</h4>
            <% } %>

            <dl class="row">
                <dt class="col-sm-3">Username</dt>
                <dd class="col-sm-9"><%= users.username %></dd>

                <dt class="col-sm-3">Email</dt>
                <dd class="col-sm-9"><%= users.email %></dd>

                <dt class="col-sm-3">Skill</dt>
                <dd class="col-sm-9"><%= users.skill %></dd>
            </dl>
        </div>
    </div>
-->

    <!-- Displays a table of recipes -->
        <div class="container pt-4">
            <div class="table-wrapper">
          	<% if (users.length > 0) {%>
              <table class="table table-hovered">
                 <thead class="thead-light">
                     <tr>
                         <th scope="col">First Name</th>
                         <th scope="col">Last Name</th>
                         <th scope="col">Email</th>
                         <th scope="col">Skill Level</th>
                         <th scope="col">Username</th>
                     </tr>
                 </thead>
                 <tbody>
                     <% users.forEach((user, index) => { %>
                         <tr>
                             <th scope="row"><%= user.firstname %></th>
                             <td><%= user.lastname %></td>
                             <td><%= user.email %></td>
                             <td><%= user.skill %></td>
                             <td><%= user.username %></td>
                         </tr>
                     <% }) %>
                 </tbody>
             </table>
        		<% } else { %>
                    <p class="text-center">User not found.</p>
    				<% } %>
            </div>
            <hr>
        </div>

</div>
